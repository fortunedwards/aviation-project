Place local, bundled image files for the frontend in this folder.

Suggested usage:
- `src/assets/images` for static app images that ship with the client bundle
- `src/data/images.js` for shared image references and image lists used across pages
